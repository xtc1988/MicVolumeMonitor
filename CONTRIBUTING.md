# 貢献ガイドライン

マイク音量モニターへの貢献に興味を持っていただき、ありがとうございます！

## 行動規範

このプロジェクトでは、全ての参加者が互いに尊重し、建設的なコミュニケーションを心がけることを期待しています。

## 貢献方法

### バグ報告

バグを発見した場合は、以下の情報を含めてIssueを作成してください：

1. **環境情報**
   - OS: Windows 10/11 (バージョン)
   - .NET Runtime: バージョン
   - アプリバージョン

2. **再現手順**
   1. 何をしたか
   2. どうなったか
   3. どうなるべきだったか

3. **スクリーンショット**（可能であれば）

4. **ログ出力**（コンソール出力があれば）

### 機能提案

新しい機能のアイデアがある場合：

1. まず既存のIssueを検索して、同じ提案がないか確認
2. 新しいIssueを作成し、以下を記載：
   - 機能の説明
   - なぜその機能が必要か
   - どのように動作すべきか
   - （あれば）実装のアイデア

### プルリクエスト

コードの変更を提案する場合：

1. **フォークとクローン**
   ```bash
   git clone https://github.com/yourusername/MicVolumeMonitor.git
   cd MicVolumeMonitor
   ```

2. **ブランチの作成**
   ```bash
   git checkout -b feature/your-feature-name
   ```

   ブランチ名の例：
   - `feature/add-recording` - 新機能
   - `bugfix/fix-crash` - バグ修正
   - `docs/update-readme` - ドキュメント更新

3. **コーディング規約**

   - C# コーディング規約に従う
   - 意味のある変数名を使用
   - メソッドにはXMLドキュメントコメントを追加
   ```csharp
   /// <summary>
   /// メソッドの説明
   /// </summary>
   /// <param name="param">パラメータの説明</param>
   /// <returns>戻り値の説明</returns>
   ```

   - コンソールログを使用
   ```csharp
   Console.WriteLine($"[ClassName] 処理内容: {details}");
   ```

4. **コミット**
   ```bash
   git add .
   git commit -m "機能: 録音機能を追加"
   ```

   コミットメッセージの例：
   - `機能: XXXを追加`
   - `修正: XXXのバグを修正`
   - `改善: XXXのパフォーマンスを向上`
   - `ドキュメント: README.mdを更新`

5. **プッシュ**
   ```bash
   git push origin feature/your-feature-name
   ```

6. **プルリクエストの作成**
   - GitHubでプルリクエストを作成
   - 変更内容を明確に記載
   - 関連するIssue番号を記載（`Fixes #123`など）

### コードレビュー

プルリクエストは以下の観点でレビューされます：

- コードの品質と可読性
- 既存機能への影響
- パフォーマンスへの影響
- テストの有無（該当する場合）
- ドキュメントの更新（必要な場合）

## 開発環境のセットアップ

### 必要なツール

- Visual Studio 2022 または
- .NET 8.0 SDK + VS Code

### ビルドと実行

```bash
# ビルド
dotnet build

# 実行
dotnet run

# リリースビルド
dotnet build -c Release
```

### デバッグ

Visual Studioで`MicVolumeMonitor.sln`を開いてF5でデバッグ実行できます。

## ドキュメントの更新

コードの変更に伴い、以下のドキュメントも更新してください：

- `README.md` - 機能説明、使い方
- `仕様書.md` - 技術仕様
- `CHANGELOG.md` - 変更履歴

## テスト

現在、ユニットテストは実装されていませんが、以下を手動で確認してください：

- [ ] アプリケーションが正常に起動する
- [ ] 監視の開始・停止が正常に動作する
- [ ] 閾値超過時に警告が表示される
- [ ] 設定が保存・読み込みされる
- [ ] システムトレイが正常に動作する

## リリースプロセス

メンテナーのみが行います：

1. バージョン番号の更新
2. CHANGELOG.mdの更新
3. Releaseタグの作成
4. ビルドとパッケージング
5. GitHubリリースの公開

## 質問がある場合

- Issueで質問を投稿
- Discussionsを利用

## ライセンス

貢献したコードはMITライセンスの下で公開されることに同意したものとみなされます。

---

ご協力ありがとうございます！
