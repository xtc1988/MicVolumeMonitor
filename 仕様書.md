# マイク音量モニター - 技術仕様書

**バージョン**: 1.0.0
**最終更新**: 2024-11-13
**対象プラットフォーム**: Windows 10/11 (64-bit)

---

## 目次

1. [概要](#概要)
2. [システムアーキテクチャ](#システムアーキテクチャ)
3. [機能仕様](#機能仕様)
4. [技術仕様](#技術仕様)
5. [データ仕様](#データ仕様)
6. [UI仕様](#ui仕様)
7. [パフォーマンス要件](#パフォーマンス要件)
8. [セキュリティ](#セキュリティ)

---

## 概要

### プロジェクト名
マイク音量モニター (MicVolumeMonitor)

### 目的
リアルタイムでマイク入力の音量を監視し、設定した閾値を超過した際に視覚的・聴覚的に警告することで、適切な音量管理を支援する。

### 対象ユーザー
- 配信者・YouTuber（マイク音量の管理）
- 在宅勤務者（Web会議での音量管理）
- 音響技術者（環境音のモニタリング）
- 一般ユーザー（騒音レベルの確認）

---

## システムアーキテクチャ

### 全体構成

```
┌─────────────────────────────────────────┐
│          MainWindow (WPF UI)            │
│  ┌────────────────────────────────────┐ │
│  │  - 音量表示                         │ │
│  │  - メーターバー                     │ │
│  │  - 設定スライダー                   │ │
│  │  - 警告アラート                     │ │
│  └────────────────────────────────────┘ │
└──────────────┬──────────────────────────┘
               │
               ↓
     ┌─────────────────────┐
     │   AudioMonitor      │
     │  ┌───────────────┐  │
     │  │ WaveInEvent   │  │ ← NAudio
     │  │ (音声キャプチャ) │  │
     │  └───────────────┘  │
     │  ┌───────────────┐  │
     │  │ RMS計算       │  │
     │  │ dB変換        │  │
     │  └───────────────┘  │
     │  ┌───────────────┐  │
     │  │ 警告音生成    │  │
     │  │ (SignalGen)   │  │
     │  └───────────────┘  │
     └─────────────────────┘
               │
               ↓
     ┌─────────────────────┐
     │   AppSettings       │
     │  ┌───────────────┐  │
     │  │ JSON保存      │  │
     │  │ レジストリ操作 │  │
     │  └───────────────┘  │
     └─────────────────────┘
```

### クラス構成

#### 1. MainWindow.xaml / MainWindow.xaml.cs
- **役割**: UIの定義とイベント処理
- **主要メソッド**:
  - `StartMonitoring()` - 監視開始
  - `StopMonitoring()` - 監視停止
  - `OnVolumeChanged(float volume)` - 音量更新イベント
  - `OnThresholdExceeded()` - 閾値超過イベント
  - `UpdateMeterBar(float volume)` - メーターバー更新
  - `UpdateWarningUI(bool isWarning)` - 警告UI更新
  - `GetVolumeLevelDescription(float db)` - 音量レベル説明取得

#### 2. AudioMonitor.cs
- **役割**: オーディオキャプチャ、音量計算、警告音再生
- **主要メソッド**:
  - `Start()` - 録音開始
  - `Stop()` - 録音停止
  - `OnDataAvailable()` - 音声データコールバック
  - `PlayWarningSound()` - 警告音再生
  - `GetAvailableDevices()` - デバイスリスト取得
- **イベント**:
  - `VolumeChanged` - 音量変化イベント
  - `ThresholdExceeded` - 閾値超過イベント

#### 3. AppSettings.cs
- **役割**: アプリケーション設定の管理
- **主要メソッド**:
  - `Load()` - 設定読み込み
  - `Save()` - 設定保存
  - `UpdateAutoStartRegistry()` - 自動起動設定
  - `IsAutoStartEnabled()` - 自動起動状態確認

#### 4. IconGenerator.cs
- **役割**: アプリケーションアイコンの動的生成
- **主要メソッド**:
  - `CreateMicrophoneIcon(int size)` - マイクアイコン生成
  - `CreateWindowIconSource()` - ウィンドウアイコン生成

---

## 機能仕様

### 1. リアルタイム音量モニタリング

#### 音声キャプチャ
- **サンプリングレート**: 44.1kHz
- **ビット深度**: 16bit
- **チャンネル**: モノラル (1ch)
- **バッファサイズ**: 100ms

#### 音量計算
- **方式**: RMS (Root Mean Square)
- **計算式**:
  ```
  RMS = √(Σ(sample²) / sampleCount)
  dB = 20 × log₁₀(RMS / 1.0) + 90
  ```
- **正規化**: -32768〜32767 → -1.0〜1.0
- **最小閾値**: 0.0001 (log10の負の無限大を防ぐ)

#### 更新頻度
- **キャプチャ**: 100msごと
- **UI更新**: リアルタイム (Dispatcher.Invoke)
- **ログ出力**: 10dB以上の音量のみ

### 2. 閾値警告機能

#### 閾値設定
- **範囲**: 30dB〜120dB
- **刻み**: 10dB単位
- **デフォルト**: 80dB

#### 警告条件
```csharp
if (currentVolume >= threshold && timeSinceLastWarning >= 2000ms)
{
    // 警告を発動
}
```

#### 警告出力

**視覚的警告**:
- 警告アラートボックスの表示
- アイコン背景色の変化 (#DBEAFE → #FEE2E2)
- 音量テキスト色の変化 (#111827 → #DC2626)
- メーターバー色の変化 (#3B82F6 → #EF4444)

**聴覚的警告**:
- 周波数: 2000Hz / 1500Hz 交互
- パターン: 100ms音 + 50ms無音 × 10回
- 音量: 設定可能 (10%〜100%)
- クールダウン: 2000ms

#### 警告音の生成
```csharp
SignalGenerator (NAudio)
  - Type: Sin wave
  - Frequency: 2000Hz / 1500Hz
  - Gain: warningVolume × 0.9
  - Duration: 100ms per beep
```

### 3. デバイス管理

#### デバイス検出
- **API**: NAudio.Wave.WaveInEvent.DeviceCount
- **取得情報**: デバイス番号、製品名
- **フォーマット**: "{番号}: {製品名}"

#### デバイス選択
- ドロップダウンメニューから選択
- 監視中は変更不可
- 設定ファイルに保存

### 4. 設定管理

#### 保存場所
```
%APPDATA%\MicVolumeMonitor\settings.json
```

#### 設定項目
```json
{
  "Threshold": 80.0,           // 閾値 (dB)
  "WarningVolume": 0.8,        // 警告音量 (0.0-1.0)
  "AutoStart": false,          // 自動起動
  "SelectedDeviceIndex": 0     // マイク番号
}
```

#### 自動保存タイミング
- 閾値変更時
- 警告音量変更時
- マイク選択変更時
- 自動起動設定変更時

### 5. システムトレイ統合

#### トレイアイコン
- カスタム生成されたマイクアイコン (32×32px)
- ツールチップ: "マイク音量監視"

#### トレイメニュー
- 「開く」: ウィンドウを表示
- 「終了」: アプリケーション終了

#### 動作
- ウィンドウ最小化時にトレイに格納
- トレイアイコンダブルクリックで復元
- 監視中はバルーン通知を表示

### 6. 自動起動機能

#### 実装方式
- Windowsレジストリを使用
- キー: `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\Run`
- 値: `MicVolumeMonitor` = `"C:\path\to\MicVolumeMonitor.exe"`

#### 設定・解除
- チェックボックスで設定
- 実行ファイルパスを自動取得
- レジストリ操作のエラーハンドリング

---

## 技術仕様

### 開発環境

#### フレームワーク
- **.NET 8.0** (net8.0-windows)
- **WPF** (Windows Presentation Foundation)

#### 外部ライブラリ
| ライブラリ | バージョン | 用途 |
|-----------|-----------|------|
| NAudio | 2.2.1 | オーディオキャプチャ・再生 |

#### 開発ツール
- Visual Studio 2022 / .NET SDK 8.0
- C# 12.0

### ビルド設定

```xml
<PropertyGroup>
  <OutputType>WinExe</OutputType>
  <TargetFramework>net8.0-windows</TargetFramework>
  <UseWPF>true</UseWPF>
  <Nullable>enable</Nullable>
  <ImplicitUsings>enable</ImplicitUsings>
  <PlatformTarget>x64</PlatformTarget>
</PropertyGroup>
```

### 音声処理パラメータ

#### サンプリング設定
```csharp
WaveFormat = new WaveFormat(
  rate: 44100,        // 44.1kHz
  bits: 16,           // 16bit
  channels: 1         // モノラル
);
BufferMilliseconds = 100;  // 100msバッファ
```

#### dB変換式
```csharp
// RMS計算
float rms = (float)Math.Sqrt(sum / sampleCount);

// dB変換 (reference = 1.0)
float db = rms > 0.0001f
    ? 20f * (float)Math.Log10(rms / 1.0) + 90f
    : 0f;
```

**補正値**: +90dB
- RMSの0.0001〜1.0の範囲を0〜90dBにマッピング
- 実用的な音量範囲に調整

---

## データ仕様

### 設定ファイル (settings.json)

#### スキーマ
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "Threshold": {
      "type": "number",
      "minimum": 30,
      "maximum": 120,
      "default": 80
    },
    "WarningVolume": {
      "type": "number",
      "minimum": 0.0,
      "maximum": 1.0,
      "default": 0.8
    },
    "AutoStart": {
      "type": "boolean",
      "default": false
    },
    "SelectedDeviceIndex": {
      "type": "integer",
      "minimum": 0,
      "default": 0
    }
  }
}
```

#### ファイルパス
```
C:\Users\{username}\AppData\Roaming\MicVolumeMonitor\settings.json
```

### レジストリ設定 (自動起動)

#### キーパス
```
HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\Run
```

#### 値
- **名前**: MicVolumeMonitor
- **種類**: REG_SZ (文字列)
- **データ**: `"C:\path\to\MicVolumeMonitor.exe"`

---

## UI仕様

### デザインシステム

#### カラーパレット (Tailwind CSS inspired)

| 用途 | カラーコード | 説明 |
|------|-------------|------|
| Primary | #2563EB | ボタン、アクティブ状態 |
| Primary Hover | #1D4ED8 | ホバー状態 |
| Primary Pressed | #1E3A8A | 押下状態 |
| Danger | #EF4444 | 警告、閾値超過 |
| Danger Light | #FEE2E2 | 警告背景 |
| Danger Dark | #DC2626 | 警告テキスト |
| Gray 50 | #F9FAFB | 背景 |
| Gray 100 | #F3F4F6 | セカンダリ背景 |
| Gray 200 | #E5E7EB | ボーダー |
| Gray 400 | #9CA3AF | 非アクティブテキスト |
| Gray 500 | #6B7280 | セカンダリテキスト |
| Gray 700 | #374151 | リミットマーカー |
| Gray 900 | #111827 | メインテキスト |
| Blue 100 | #DBEAFE | アイコン背景 |

#### タイポグラフィ

| 要素 | フォントサイズ | フォントウェイト |
|------|--------------|----------------|
| 音量数値 | 72pt | Bold |
| タイトル | 18pt | SemiBold |
| ボタン | 14pt | SemiBold |
| ラベル | 13pt | Regular |
| 説明 | 12-13pt | Regular |
| 単位表示 | 32pt | Regular |

#### レイアウト

**ウィンドウサイズ**:
- 幅: 480px (固定)
- 高さ: 850px (固定)

**カード**:
- 背景: White (#FFFFFF)
- 角丸: 12px
- ボーダー: 1px solid #E5E7EB
- シャドウ: 0px 0px 20px rgba(0,0,0,0.15)

**間隔**:
- セクション間: 24px
- 要素間: 8px〜16px

### コンポーネント仕様

#### 1. 音量表示エリア
- **現在の音量**: 72pt, Bold
- **単位 (dB)**: 32pt, グレー
- **アクティビティインジケーター**: 監視中は青色の●
- **情報テキスト**: "上限: XXdB / ピーク: XXdB"

#### 2. メーターバー
- **高さ**: 12px
- **角丸**: 6px
- **背景**: #E5E7EB
- **メーター色**: #3B82F6 (通常) / #EF4444 (警告)
- **リミットマーカー**: 2px幅、#374151

#### 3. スライダー
- **範囲表示**: 右上に背景付きバッジ
- **刻み**: 10単位
- **スナップ**: 有効

#### 4. ボタン
- **高さ**: 40px
- **角丸**: 6px
- **ホバー効果**: 色変化
- **押下効果**: 色変化 + スケール (0.98)

#### 5. 警告アラート
- **背景**: #FEF2F2
- **ボーダー**: 1px solid #FECACA
- **アイコン**: ⚠ (16pt, #DC2626)
- **表示条件**: 閾値超過時のみ

---

## パフォーマンス要件

### レスポンス時間

| 操作 | 目標時間 | 実測値 |
|------|---------|--------|
| アプリ起動 | < 2秒 | 約1秒 |
| 監視開始 | < 500ms | 約200ms |
| UI更新 | < 50ms | リアルタイム |
| 警告音再生 | < 100ms | 即座 |

### リソース使用量

| リソース | 通常時 | 監視中 |
|---------|--------|--------|
| CPU | < 1% | < 3% |
| メモリ | 約30MB | 約40MB |
| ディスクI/O | 最小限 | 設定保存時のみ |

### スレッド構成
- **UIスレッド**: メインウィンドウ、イベント処理
- **オーディオキャプチャスレッド**: NAudioによる自動管理
- **警告音再生スレッド**: ThreadPool使用

---

## セキュリティ

### アクセス権限

#### 必要な権限
- **マイクアクセス**: 音声入力デバイスへのアクセス
- **ファイルシステム**: %APPDATA%への読み書き
- **レジストリ**: HKEY_CURRENT_USERへの書き込み

#### 不要な権限
- 管理者権限は不要
- ネットワークアクセスなし
- 他のアプリケーションへのアクセスなし

### データプライバシー

- **音声データ**: メモリ上でのみ処理、保存しない
- **設定データ**: ローカルのみ、外部送信なし
- **ログデータ**: コンソール出力のみ、ファイル保存なし

### コード品質

#### エラーハンドリング
```csharp
try {
    // 処理
} catch (Exception ex) {
    Console.WriteLine($"[Class] エラー - {ex.Message}");
    // フォールバック処理
}
```

#### リソース管理
- `IDisposable`パターンの実装
- `using`ステートメントの使用
- ネイティブリソースの明示的解放

---

## 拡張性

### 将来的な機能追加の可能性

1. **録音機能**: 閾値超過時の音声を自動録音
2. **履歴表示**: 過去の音量履歴をグラフ表示
3. **複数閾値**: 警告・注意など複数レベルの設定
4. **カスタム警告音**: WAVファイルからの警告音読み込み
5. **ネットワーク通知**: Discord/Slack連携
6. **設定プリセット**: 用途別の設定プリセット
7. **多言語対応**: 英語・中国語などのサポート

### プラグインアーキテクチャ
現在は非対応。将来的にIPluginインターフェースを検討。

---

## テスト仕様

### ユニットテスト

#### AudioMonitor.cs
- [ ] GetAvailableDevices() - デバイスリスト取得
- [ ] Start() / Stop() - 開始・停止の正常動作
- [ ] OnDataAvailable() - 音量計算の正確性

#### AppSettings.cs
- [ ] Load() / Save() - 設定の読み書き
- [ ] UpdateAutoStartRegistry() - レジストリ操作

### 統合テスト
- [ ] 監視開始から警告までのフロー
- [ ] 設定変更の永続化
- [ ] システムトレイの動作

### 手動テスト
- [ ] 各dBレベルでの警告動作
- [ ] 長時間動作の安定性
- [ ] マイク切り替え
- [ ] ウィンドウ最小化・復元

---

## 既知の制限事項

1. **Windows専用**: macOS/Linuxでは動作しない
2. **モノラルのみ**: ステレオマイクは左チャンネルのみ使用
3. **警告音クールダウン**: 2秒間隔で固定
4. **dB計算**: 絶対値ではなく相対値

---

## 参考資料

### 音響関連
- RMS (Root Mean Square): https://en.wikipedia.org/wiki/Root_mean_square
- デシベル (dB): https://ja.wikipedia.org/wiki/デシベル

### ライブラリ
- NAudio: https://github.com/naudio/NAudio
- WPF: https://learn.microsoft.com/ja-jp/dotnet/desktop/wpf/

### デザイン
- Tailwind CSS Colors: https://tailwindcss.com/docs/customizing-colors

---

**文書管理**

| 項目 | 内容 |
|------|------|
| 文書番号 | MVM-SPEC-001 |
| バージョン | 1.0.0 |
| 作成日 | 2024-11-13 |
| 最終更新日 | 2024-11-13 |
| 承認者 | - |
